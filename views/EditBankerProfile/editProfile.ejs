<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Profile</title>
    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h2 class="mb-4">Edit Profile</h2>

      <form action="/banker/editProfile/<%= id %>" method="post">
        <div class="mb-3">
          <label for="fullName" class="form-label">First Name</label>
          <input
            type="text"
            value="<%= banker.firstName %>"
            class="form-control"
            name="firstName"
            id="fullName"
            placeholder="Enter your full name"
            required
          />
        </div>

        <div class="mb-3">
          <label for="fullName" class="form-label">Last Name</label>
          <input
            type="text"
            value="<%= banker.lastName %>"
            class="form-control"
            name="lastName"
            id="fullName"
            placeholder="Enter your full name"
            required
          />
        </div>

        <div class="mb-3">
          <label for="panNumber" class="form-label">PAN Number</label>
          <input
            type="text"
            value="<%= banker.pan %>"
            class="form-control"
            name="pan"
            id="panNumber"
            placeholder="Enter your PAN number"
            required
          />
        </div>

        <div class="mb-3">
          <label for="rmName" class="form-label">Email</label>
          <input
            type="text"
            value="<%= banker.email %>"
            class="form-control"
            name="email"
            id="rmName"
          />
        </div>

        <div class="mb-3">
          <label for="rmName" class="form-label">Account Number</label>
          <input
            type="text"
            value="<%= banker.bankDetails.accountNumber %>"
            class="form-control"
            name="accountNumber"
            id="rmName"
          />
        </div>

        <div class="mb-3">
          <label for="rmName" class="form-label">IFSC Code</label>
          <input
            type="text"
            value="<%= banker.bankDetails.ifsc %>"
            class="form-control"
            name="ifsc"
            id="rmName"
          />
        </div>

        <div class="mb-3">
          <label for="rmPhone" class="form-label"
            >Relationship Manager Phone</label
          >
          <input type="tel class="form-control" id="rmPhone" value="<%=
          banker.phone %>">
        </div>

        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
    </div>

    <!-- Bootstrap JS (optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
